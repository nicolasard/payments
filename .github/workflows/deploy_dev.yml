
name: Deploy dev environment job.

on:
  workflow_dispatch:

jobs:
  deploy_dev:
    runs-on: ln-moni01

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ github.event.inputs.name }}"
